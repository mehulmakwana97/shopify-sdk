import { Resource } from "../../common/Resource";
export declare class Metafields extends Resource {
    /**
     * Retrieves a list of metafields that belong to a resource
     */
    constructor(config: any, id?: string);
}
